const controller = require('./like.controller');
const service = require('./like.service');
const dal = require('./like.dal');
const validation = require('./like.validation');

module.exports = {
  controller,
  service,
  dal,
  validation,
};
