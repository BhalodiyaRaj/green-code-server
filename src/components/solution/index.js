const controller = require('./solution.controller');
const service = require('./solution.service');
const dal = require('./solution.dal');
const validation = require('./solution.validation');

module.exports = {
  controller,
  service,
  dal,
  validation,
};
