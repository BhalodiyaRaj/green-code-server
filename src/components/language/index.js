const controller = require('./language.controller');
const service = require('./language.service');
const dal = require('./language.dal');
const validation = require('./language.validation');

module.exports = {
  controller,
  service,
  dal,
  validation,
};
